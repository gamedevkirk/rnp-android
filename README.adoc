= RNP

== Android Support

The `android-v0.18.1` branch contains the necessary changes to support Android. Only `arm64-v8a` has been tested, but other architectures should work  fine as long as you supply supporting OpenSSL binaries.
