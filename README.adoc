= RNP

== Android Support

Only `arm64-v8a` has been tested, but other architectures should work  fine as long as you supply supporting OpenSSL binaries. Modify the `build-android.sh` script as necessary for your configuration.
